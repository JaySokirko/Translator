<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/input_text_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <FrameLayout
        android:id="@+id/attached_anchor2"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="200dp"
        android:layout_marginEnd="10dp"
        android:layout_marginStart="10dp"
        android:layout_marginTop="50dp"
        android:background="@drawable/view_rounded">


        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:ignore="UselessParent">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="10dp"
                android:layout_marginStart="10dp"
                android:layout_marginTop="10dp"
                android:paddingBottom="30dp"
                android:text="@string/long_text"
                android:textColor="@color/colorText"
                android:textSize="16sp" />

        </ScrollView>

    </FrameLayout>


    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_translation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/fab_margin"
        android:onClick="onTranslateClickListener"
        app:layout_anchor="@id/attached_anchor2"
        app:layout_anchorGravity="bottom|end"
        app:srcCompat="@drawable/ic_action_translation" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_clear_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/fab_margin"
        android:onClick="onTranslateClickListener"
        app:layout_anchor="@id/attached_anchor2"
        app:layout_anchorGravity="bottom|center"
        app:srcCompat="@drawable/ic_action_clear_text" />

</android.support.design.widget.CoordinatorLayout>